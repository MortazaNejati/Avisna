# گزارش رسمی پیشرفت پروژه
## توسعه و تکمیل نسخه پنجم زبان Avisna/Lume

**تاریخ گزارش:** ۱۳ سپتامبر ۲۰۲۶  
**موضوع:** گزارش فعالیت‌های انجام‌شده، خطاهای برطرف‌شده، محدودیت‌های شناسایی‌شده و برنامه کاری روز آینده

---

## ۱. شرح کار انجام‌شده

در این مرحله از توسعه نسخه پنجم زبان **Avisna/Lume**، تمرکز اصلی بر توسعه قابلیت‌های هندسی Native در هسته زبان و هماهنگ‌سازی لایه‌های مختلف کامپایلر قرار گرفت.

فعالیت‌ها در چند لایه انجام شد:

### ۱-۱. توسعه انواع پایه هندسی

سه نوع جدید به سیستم نوع زبان اضافه و در بخش‌های مختلف کامپایلر پشتیبانی شدند:

- `Point`
- `Vector`
- `Angle`

این انواع در AST زبان ثبت شدند و سپس پشتیبانی لازم برای تشخیص و Resolve شدن آن‌ها در Semantic و Formatter اضافه گردید.

### ۱-۲. نگاشت انواع هندسی به LLVM

در CodeGen، نگاشت انواع هندسی به ساختارهای LLVM انجام شد.

برای `Point` و `Vector`، ساختار Native شامل دو مقدار `f64` برای مختصات `x` و `y` ایجاد شد.

به‌صورت مفهومی:

```text
Point  = { f64 x, f64 y }
Vector = { f64 x, f64 y }
```

در لایه ABI داخلی CodeGen نیز Pointer مربوط به این ساختارها به‌صورت `i64` منتقل می‌شود.

### ۱-۳. پیاده‌سازی Constructorهای هندسی

Constructorهای زیر در CodeGen پشتیبانی شدند:

```text
point(x, y)
vector(x, y)
```

این توابع ساختار Native متناظر را ایجاد کرده و مختصات را در آن ذخیره می‌کنند.

### ۱-۴. پشتیبانی از Field Access

دسترسی به مؤلفه‌های هندسی نیز پیاده‌سازی و آزمایش شد:

```avs
p.x
p.y

v.x
v.y
```

Semantic نوع `x` و `y` را برای `Point` و `Vector` به‌صورت `F64` تشخیص می‌دهد و CodeGen نیز دسترسی به فیلدهای Native را انجام می‌دهد.

### ۱-۵. توسعه عملیات Vector

عملیات اصلی روی `Vector` پیاده‌سازی و تست شدند:

- `Vector + Vector`
- `Vector - Vector`
- `Vector * Scalar`
- `Scalar * Vector`
- `Vector / Scalar`

در این بخش، منطق Semantic و CodeGen هماهنگ شد تا نتیجه عملیات مطابق نوع هندسی مورد انتظار باشد.

### ۱-۶. توسعه عملیات Point و Vector

عملیات ترکیبی بین `Point` و `Vector` نیز توسعه یافتند:

- `Point - Point → Vector`
- `Point - Vector → Point`
- `Point + Vector → Point`
- `Vector + Point → Point`

برای عملیات فوق، علاوه بر Semantic، مسیر CodeGen Native نیز تکمیل شد.

### ۱-۷. اصلاح سیستم تشخیص نوع عملیات

در `crates/avisna_codegen/src/expr/operators.rs` منطق تشخیص نوع نتیجه عملیات هندسی تکمیل شد.

به‌عنوان نمونه، برای:

```text
Vector + Point
```

نتیجه باید:

```text
Point
```

باشد.

این موضوع در منطق تعیین نوع عبارت لحاظ شد و سپس مسیر Native CodeGen مربوط به آن نیز اضافه گردید.

### ۱-۸. اصلاح Regression مربوط به Bool و Match

در جریان تست‌های قبلی، یک ناسازگاری در مقایسه مقادیر `Bool` در `match` شناسایی شد.

LLVM در یک مسیر، مقدار `Bool` را با `i32` مقایسه می‌کرد، در حالی که مقدار صحیح Bool در LLVM به‌صورت `i1` است.

این بخش اصلاح شد تا مقایسه الگوهای:

```avs
true
false
```

با نوع صحیح LLVM انجام شود.

---

# ۲. خطاهای برطرف‌شده

تا پایان این مرحله، خطاهای زیر شناسایی و برطرف شده‌اند.

### ۲-۱. عدم شناسایی انواع هندسی

مشکل مربوط به نبود انواع:

```text
Point
Vector
Angle
```

در سیستم نوع زبان برطرف شد.

### ۲-۲. خطای Formatter

اضافه شدن انواع جدید باعث ایجاد خطای exhaustive match در Formatter شده بود.

این مورد با اضافه کردن پردازش انواع هندسی به Formatter برطرف شد.

### ۲-۳. عدم Resolve شدن انواع هندسی در Semantic

تشخیص نام‌های:

```text
Point
Vector
Angle
```

در سیستم Resolve نوع اضافه و تست شد.

### ۲-۴. مشکلات LLVM Type Mapping

نگاشت انواع هندسی به نوع مناسب LLVM و ایجاد ساختارهای Native مربوط به `Point` و `Vector` انجام شد.

### ۲-۵. مشکل Storage متغیرهای هندسی

نحوه ذخیره `Point` و `Vector` در متغیرهای محلی CodeGen تکمیل شد.

### ۲-۶. مشکل Bool در LLVM

Regression مربوط به مقایسه `Bool` در `match` اصلاح شد.

تست:

```avs
fn main() -> int {
    let x = true;
    return match x {
        true => 1,
        false => 0,
    };
}
```

پس از اصلاح بدون خطا اجرا شد.

### ۲-۷. عملیات Vector

عملیات زیر پس از اصلاحات لازم با موفقیت تست شدند:

```text
Vector + Vector
Vector - Vector
Vector * Scalar
Scalar * Vector
Vector / Scalar
```

### ۲-۸. عملیات Point - Point

عملیات:

```text
Point - Point
```

با نتیجه:

```text
Vector
```

پیاده‌سازی و آزمایش شد.

### ۲-۹. عملیات Point - Vector

عملیات:

```text
Point - Vector
```

با نتیجه:

```text
Point
```

در Semantic و CodeGen پشتیبانی شد.

### ۲-۱۰. عملیات Point + Vector

عملیات:

```text
Point + Vector
```

با نتیجه:

```text
Point
```

تکمیل شد.

### ۲-۱۱. عملیات Vector + Point

آخرین مورد بررسی‌شده:

```text
Vector + Point
```

بود که در ابتدا با خطای Semantic زیر مواجه شد:

```text
error[E105]: arithmetic operation requires numeric operands
```

پس از تکمیل مسیرهای مربوطه، خطا برطرف شد و تست با موفقیت انجام شد.

---

# ۳. خطاهای برطرف‌نشده

در پایان کار امروز، در محدوده فعالیت‌های انجام‌شده **خطای فعال و گزارش‌شده‌ای که مانع ادامه تست‌های هندسی باشد باقی نمانده است**.

با این حال، این موضوع به معنی تکمیل کامل قابلیت‌های هندسی یا نسخه پنجم نیست. مواردی که هنوز باید بررسی یا توسعه داده شوند در بخش محدودیت‌ها و برنامه فردا آمده‌اند.

---

# ۴. محدودیت‌های دیده‌شده

در جریان توسعه مشخص شد که پشتیبانی از انواع Native جدید تنها با اضافه کردن Type به AST کافی نیست و باید چندین لایه به‌صورت هماهنگ اصلاح شوند.

این لایه‌ها شامل:

```text
AST
  ↓
Formatter
  ↓
Semantic
  ↓
Type Resolution
  ↓
Storage
  ↓
LLVM Type Mapping
  ↓
Native Structs
  ↓
Expression CodeGen
```

هستند.

همچنین مشخص شد که ABI فعلی Expression CodeGen برای برخی انواع پیچیده، مقدار را به‌شکل `i64` منتقل می‌کند و برای `Point` و `Vector`، این مقدار در عمل نماینده Pointer به ساختار Native است.

این موضوع باید در توسعه قابلیت‌های بعدی، خصوصاً هنگام بررسی Return Type و Function Parameterهای هندسی، با دقت در نظر گرفته شود.

### محدودیت مهم دیگر

پشتیبانی کامل Functionها برای بازگرداندن مستقیم انواع:

```text
Point
Vector
```

هنوز موضوع مستقلی است و در این مرحله هدف اصلی نبوده است.

در نتیجه، توسعه عملیات Expression-Level هندسی انجام شده، اما نباید آن را به‌عنوان تکمیل کامل ABI توابع هندسی تلقی کرد.

### محدودیت مربوط به Angle

نوع `Angle` به سیستم نوع و LLVM اضافه شده است، اما عملیات ریاضی و معنایی کامل آن هنوز در این مرحله توسعه داده نشده است.

---

# ۵. وضعیت فعلی پروژه

در پایان این مرحله، زیرساخت اولیه Geometry Native در زبان شکل گرفته و عملیات اصلی موردنیاز برای `Point` و `Vector` در سطح Expression پیاده‌سازی شده‌اند.

وضعیت کلی:

| قابلیت | وضعیت |
|---|---|
| `Point` در AST | انجام شد |
| `Vector` در AST | انجام شد |
| `Angle` در AST | انجام شد |
| Formatter | انجام شد |
| Semantic Type Resolution | انجام شد |
| LLVM Mapping | انجام شد |
| Native Point Struct | انجام شد |
| Native Vector Struct | انجام شد |
| `point(x,y)` | انجام شد |
| `vector(x,y)` | انجام شد |
| `Point.x / Point.y` | انجام شد |
| `Vector.x / Vector.y` | انجام شد |
| `Vector + Vector` | انجام شد |
| `Vector - Vector` | انجام شد |
| `Vector * Scalar` | انجام شد |
| `Scalar * Vector` | انجام شد |
| `Vector / Scalar` | انجام شد |
| `Point - Point` | انجام شد |
| `Point - Vector` | انجام شد |
| `Point + Vector` | انجام شد |
| `Vector + Point` | انجام شد |
| Bool/Match regression | برطرف شد |
| عملیات کامل `Angle` | باقی مانده |
| ABI کامل Point/Vector در توابع | باقی مانده |

---

# ۶. شرح کار فردا

تمرکز مرحله بعد بر ادامه توسعه قابلیت‌های هندسی و بررسی کامل‌تر یکپارچگی آن‌ها با زبان خواهد بود.

اولویت‌های پیشنهادی:

### ۶-۱. تکمیل عملیات هندسی باقی‌مانده

عملیات موردنیاز برای `Point` و `Vector` که هنوز در Scope توسعه قرار نگرفته‌اند، بررسی خواهند شد.

از جمله:

- عملیات Unary
- مقایسه‌ها در صورت نیاز زبان
- ترکیب Geometry با سایر عملیات عددی
- بررسی رفتار Type Inference در حالت‌های مختلف

### ۶-۲. بررسی و توسعه `Angle`

پس از تثبیت `Point` و `Vector`، قابلیت‌های نوع:

```text
Angle
```

بررسی و برای آن Semantic و CodeGen مناسب طراحی خواهد شد.

### ۶-۳. بررسی ABI توابع هندسی

نحوه استفاده از:

```text
Point
Vector
Angle
```

به‌عنوان:

- Function Parameter
- Function Return Type
- مقدار محلی
- مقدار برگشتی Expression

بررسی خواهد شد تا مشخص شود ABI فعلی چه بخش‌هایی را پوشش می‌دهد و چه اصلاحاتی لازم است.

### ۶-۴. تکمیل تست‌های Regression

برای هر عملیات هندسی، تست‌های مستقل در مسیر Backend ایجاد یا تکمیل خواهند شد تا از بازگشت خطاهای فعلی در تغییرات بعدی جلوگیری شود.

### ۶-۵. ادامه توسعه نسخه پنجم

پس از تثبیت بخش Geometry، کار بر روی سایر موارد تعریف‌شده برای نسخه پنجم، از جمله قابلیت‌های Semantic و کاهش Syntax اضافی زبان، ادامه خواهد یافت.

---

## جمع‌بندی

در پایان این مرحله، زیرساخت Native Geometry در Avisna/Lume به‌طور قابل‌توجهی توسعه یافته است.

سه نوع `Point`، `Vector` و `Angle` وارد سیستم نوع شده‌اند و مسیرهای اصلی AST، Semantic و LLVM برای آن‌ها ایجاد شده است. همچنین عملیات اصلی `Point` و `Vector` در سطح Expression پیاده‌سازی و آزمایش شده‌اند.

مهم‌ترین نتیجه این مرحله، عبور موفق از مشکلات مربوط به هماهنگ‌سازی **Type System، Semantic Checker و LLVM Code Generator** برای انواع هندسی بوده است.

در حال حاضر، پایه لازم برای ادامه توسعه Geometry و حرکت به سمت قابلیت‌های پیشرفته‌تر زبان فراهم شده است.